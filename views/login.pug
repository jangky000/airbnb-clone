doctype
html(lang='ko')
    head
        meta(charset='utf-8')
        link(rel="stylesheet", href="../css/style.css")
        title= "숙소, 체험, 장소를 모두 한 곳에서 - 에어비앤비"
    body
        header#header
            include ./sub/header.pug
        main#main= "메인"
            div.container
                div
                    h2= "로그인"
                    form(action="./login", method="post")
                        //- input#id(type="text", name="id", placeholder="아이디")
                        ul
                            li
                                input#email(type="email", name="email", placeholder="이메일")
                            li 
                                input#pwd(type="password", name="pwd", placeholder="패스워드")
                            li
                                button(type="submit")= "로그인"
                                button(type="button", onclick="location.href='./register'")= "회원가입"
